Django==4.2.0           # Pinned to a stable LTS version
scikit-learn==1.4.1.post1  # Must match model training version
joblib==1.3.2           # Often used with scikit-learn models
fpdf==1.7.2             # Pinned to avoid breaking changes
gunicorn==21.2.0        # Stable production server
gdown==4.7.1            # For Google Drive downloads
djangorestframework==3.14.0  # Compatible with Django 4.2
numpy==1.24.4           # Required by scikit-learn (pinned for safety)
pandas==2.0.3           # Often needed for ML/data handling
